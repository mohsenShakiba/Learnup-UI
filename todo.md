# remember if each of these tasks require an api update or a missing update, just ignore the task. otherwise complete the task and remove them from the list.

[] add a logout button to settings.
[] in ReaderTranslationDrawer, add a bookmark button that will search for the provided word. if the provided word exists, add it to litner box, otherwise create the vocab using VocabService then add the created vocab id to leitner box.
[] in course detail page, move the timeline to the right and user farsi lesson titles